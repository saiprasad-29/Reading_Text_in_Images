torch>=2.1
torchvision>=0.16
transformers>=4.40
pillow>=10.0
numpy>=1.24
pandas>=2.0
pyyaml>=6.0
tqdm>=4.66
matplotlib>=3.8
# OCR engines (install at least one)
pytesseract>=0.3.10        # also: apt-get install tesseract-ocr
easyocr>=1.7.1
# optional, only if you use the API-based VLM
anthropic>=0.40
